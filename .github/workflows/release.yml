name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    # steps:
    #   - name: Checkout code
    #     uses: actions/checkout@v3

    #   - name: Set up Node.js
    #     uses: actions/setup-node@v3
    #     with:
    #       node-version: '18'

    #   - name: Install dependencies
    #     run: npm install

    #   - name: Build extension
    #     run: npm run build

    #   - name: Publish extension
    #     uses: vsce-action@v1
    #     with:
    #       args: publish --packagePath ./out
          
